# アプリの起動・実行手順

本アプリは、動作しているAndroid端末自身の「Android Auto」と通信してProjectionを行います。
以下の手順に従って、実機またはエミュレータでアプリを起動してください。

## 1. Android Auto 開発者向け設定の有効化
まず、動作させるAndroid端末（実機・エミュレータ問わず）のAndroid Autoの設定を変更します。

1. 端末の「設定」>「接続済みのデバイス」>「接続の設定」>「Android Auto」を開きます（端末によって「設定」>「アプリ」>「Android Auto」の場合もあります）。
2. 画面の一番下までスクロールし、「バージョン」部分を **10回連続でタップ** します。
3. 「開発者向けの設定を有効にしますか？」と聞かれるので「OK」を選択します。

## 2. ヘッドユニットサーバーの起動
本アプリが接続するためのサーバーをAndroid端末上で立ち上げます。

1. Android Auto設定画面の右上にある「3点リーダー（︙）」メニューをタップします。
2. **「ヘッドユニットサーバーを起動（Start head unit server）」** を選択します。
3. 端末の通知領域（ステータスバーを下にスワイプした画面）に、「**Android Auto - ヘッドユニットサーバーが実行中です**」という通知が表示されていることを確認します。

> [!IMPORTANT]
> サーバーが起動していない状態（通知が出ていない状態）では、本アプリから接続ができずエラーとなります。

## 3. アプリのビルドと起動
サーバーが起動している状態で、本アプリを実行します。

1. Android Studio 等で本プロジェクトをビルドし、**ヘッドユニットサーバーを起動したのと同じAndroid端末（実機またはエミュレータ）** へインストールして実行します。
2. アプリが起動すると、自動的に `127.0.0.1:5277` へTCP接続を試みます。
3. 接続に成功すると、AAP Sessionが確立され、画面上にAndroid AutoのUIが表示（Projection）されます。

> [!NOTE]
> エミュレータを使用する場合も、実機と全く同じ手順でエミュレータ内のAndroid Autoからサーバーを起動し、エミュレータ内で本アプリを実行してください。
